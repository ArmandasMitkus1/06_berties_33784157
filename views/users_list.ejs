<h1>Users — <%= shopName %></h1>

<p>
    <a href="<%= basePath %>/">Home</a> |
    <a href="<%= basePath %>/books">Books</a> |
    <a href="<%= basePath %>/cart">Cart</a> |
    <a href="<%= basePath %>/logout">Logout</a>
</p>
<hr>

<table border="1" cellpadding="6">
<tr><th>User</th><th>First</th><th>Last</th><th>Email</th></tr>
<% users.forEach(u => { %>
<tr>
<td><%= u.username %></td><td><%= u.first_name %></td><td><%= u.last_name %></td><td><%= u.email %></td>
</tr>
<% }) %>
</table>
