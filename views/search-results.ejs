<!DOCTYPE html>
<html>
<head>
    <title>Search Results – <%= shopName %></title>
    <link rel="stylesheet" href="<%= basePath %>/main.css">
</head>
<body>

    <h1>🔍 Search Results for "<%= keyword %>"</h1>

    <p>
        <a href="<%= basePath %>/">🏠 Home</a> |
        <a href="<%= basePath %>/books">📚 All Books</a> |
        <a href="<%= basePath %>/users/list">👤 User List</a> |
        <a href="<%= basePath %>/logout">🚪 Logout</a>
    </p>

    <% if (results && results.length > 0) { %>
        <ul>
            <% results.forEach(book => { %>
                <li>
                    <a href="<%= basePath %>/books/<%= book.id %>"><%= book.name %></a>
                    — £<%= Number(book.price).toFixed(2) %>
                    <a href="<%= basePath %>/cart/add/<%= book.id %>" style="margin-left:10px;">🛒 Add to Cart</a>
                </li>
            <% }) %>
        </ul>
    <% } else { %>
        <p>No results found.</p>
    <% } %>

</body>
</html>
