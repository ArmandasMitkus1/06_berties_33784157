<!DOCTYPE html>
<html>
<head>
  <title>Books - <%= shopName %></title>
  <link rel="stylesheet" href="main.css">
</head>
<body>
  <h1>📚 Books at <%= shopName %></h1>
  
  <p><a href="<%= basePath %>/">🏠 Back to Home</a> | <a href="<%= basePath %>/users/list">User List</a> | <a href="<%= basePath %>/logout">Logout</a></p>


  <% if (books && books.length > 0) { %>
    <ul>
      <% books.forEach(book => { %>
        <li>
          <a href="<%= basePath %>/books/<%= book.id %>"><%= book.name %></a>
          (£<%= Number(book.price).toFixed(2) %>)
        </li>
      <% }) %>
    </ul>
  <% } else { %>
    <p>No books found in the database.</p>
  <% } %>
</body>
</html>