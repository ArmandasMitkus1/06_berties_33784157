<!DOCTYPE html>
<html>
<head>
  <title>Books - <%= shopName %></title>
  <link rel="stylesheet" href="/main.css">
</head>
<body>

<h1>📚 Books at <%= shopName %></h1>

<p>
  <a href="/">🏠 Home</a> | 
  <a href="/users/list">Users</a> | 
  <a href="/cart">🛒 Cart</a> | 
  <a href="/logout">Logout</a>
</p>

<% if (books && books.length > 0) { %>
  <ul>
    <% books.forEach(book => { %>
      <li>
        <a href="/books/<%= book.id %>"><%= book.name %></a> — £<%= Number(book.price).toFixed(2) %>
        <a href="/cart/add/<%= book.id %>" style="margin-left:10px">🛒 Add to Cart</a>  <!-- NEW -->
      </li>
    <% }) %>
  </ul>
<% } else { %>
  <p>No books available.</p>
<% } %>

</body>
</html>
