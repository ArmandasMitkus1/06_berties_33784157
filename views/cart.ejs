<h1>Your Cart 🛒 — <%= shopName %></h1>

<p>
  <a href="<%= basePath %>/">🏠 Home</a> |
  <a href="<%= basePath %>/books">📚 Books</a> |
  <a href="<%= basePath %>/users/list">Users</a> |
  <a href="<%= basePath %>/logout">Logout</a>
</p>
<hr>

<% if (!cart || cart.length === 0) { %>
  <h2>Cart is empty</h2>
  <p><a href="<%= basePath %>/books">Browse books</a></p>

<% } else { %>

<table border="1" cellpadding="6">
<tr><th>ID</th><th>Name</th><th>Price</th><th>Remove</th></tr>

<% let total = 0; cart.forEach(item => { total += Number(item.price); %>
<tr>
  <td><%= item.id %></td>
  <td><%= item.name %></td>
  <td>£<%= Number(item.price).toFixed(2) %></td>
  <td><a href="<%= basePath %>/cart/remove/<%= item.id %>">❌</a></td>
</tr>
<% }) %>
</table>

<h2>Total: £<%= total.toFixed(2) %></h2>
<button>Checkout 💳</button>

<% } %>
